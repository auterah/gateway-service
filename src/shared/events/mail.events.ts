export enum MailEvents {
  PUSH_MAIL = 'push.mail',
  SEND_MAIL = 'send.mail',
  GET_MAIL = 'get.mail',
  SET_SMTP = 'set.smtp',
  SET_MAIL_SENDER = 'set.mail.sender',
  MAIL_QUEUED = 'mail.queue',
  QUEUE_MAIL = 'queue.mail',
  MAIL_FAILED = 'mail.failed',
  PUSH_BULK_MAIL = 'push.bulk.mail',
  MAIL_SENT = 'mail.sent',
  MEMORIZE = 'memorize',
}

