export enum AppRequestEvents {
  NEW_REQUEST = 'New App Request',
}

export enum AppEvents {
  GET_APP = 'Get.App',
  CONFIRM_ACTION = 'Confirm.action', // Confirms that app has permission to execute action
  CREATED = 'Created.App',
}
